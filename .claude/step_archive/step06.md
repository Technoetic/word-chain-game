# Step 06 - GitHub 조사

## 🚨 절대 금지
- **어떤 이유로도 이 단계를 스킵할 수 없다.**
- "신규 프로젝트", "해당 없음", "참고할 레포 없음" 등의 이유로 스킵하는 것은 엄격히 금지된다.
- 반드시 GitHub API를 실제로 호출하고 결과를 `.claude/step06_조사결과_chunk1.md`에 저장해야 한다.

합리적인 선에서 최대한 많은 서브에이전트를 병렬로 사용해 오직 GitHub 조사만을 수행한다.

## 주의사항

- **이 단계에서 절대로 "planning.md"를 생성하지 않는다. 오직 조사만 한다.**
- **이 단계에서 절대로 "design.md"를 생성하지 않는다. 오직 조사만 한다.**
- **이 단계에서 절대로 plan mode를 사용하지 않는다.**

**조사 결과는 청크 단위로 저장한다:**

```
step06_조사결과_chunk1.md (500줄 이하)
step06_조사결과_chunk2.md (500줄 이하)
step06_조사결과_chunk3.md (500줄 이하)
...
```

**작성 규칙**:

- 각 청크는 500줄 이하로 작성 (성능 최적화)
- `.claude/hooks/research-validator.ps1`에서 각 청크 검증 (BOM/CRLF/줄수/파일크기)
- 청크 그대로 유지 (병합 안 함)

서브에이전트는 항상 haiku를 사용한다.

## 웹 요청 진행률 정책

1. **WebFetch 사용 금지**

   - WebFetch 도구는 절대 사용하지 않는다
   - 모든 웹 요청은 Bash 도구로 처리한다
2. **Bash 도구 사용 필수**

   - 모든 웹 데이터 수집은 curl 또는 wget 사용
   - 반드시 백그라운드 모드로 실행: `run_in_background: true`
   - 진행률 옵션 필수 적용: curl -# 또는 wget --progress=bar:force
3. **진행률 실시간 보고**

   - 작업 시작 즉시 사용자에게 알림
   - BashOutput 도구로 주기적으로 진행 상태 확인
   - 진행률이 100%로 완료될 때까지 계속 보고
   - 완료 시 최종 결과 보고

---

이 지침을 완료한 즉시 자동으로 step07.md를 읽고 수행한다. 사용자 확인을 기다리지 않는다.
