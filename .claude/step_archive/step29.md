# Step 29 - 타입 안전성

프로젝트 언어와 타입 시스템 특성을 분석하여 적절한 타입 안전성 전략을 동적으로 결정한다.

## 작업 흐름

1. **타입 체크 실행**
   - 프로젝트 타입 설정에 따라 타입 체크 실행
   - 타입 오류 및 경고 확인

2. **타입 안전성 개선**
   - 타입이 없는 코드에 타입 추가
   - 불안전한 타입(any, unknown 등) 개선
   - 타입 오류 수정

합리적인 선에서 최대한 많은 서브에이전트를 병렬로 사용하여 타입 안전성을 확보한다.

**타입 안전성 단계에서 절대로 plan mode를 사용하지 않는다.**

**객관적 검증 기준:**
- 타입 체크 통과 (언어별 타입 체커)
- 타입 오류 0개

**검증:**
- `.claude/hooks/type-safety-validator.ps1`에서 자동 검증

**검증 실패 시:**
- 타입 오류 원인을 분석
- 타입을 수정
- 검증 통과할 때까지 반복

**타입 안전성 결과는 청크 단위로 저장한다:**

```
step29_타입안전성_chunk1.md (500줄 이하)
step29_타입안전성_chunk2.md (500줄 이하)
step29_타입안전성_chunk3.md (500줄 이하)
...
```

**작성 규칙**:
- 각 청크는 500줄 이하로 작성 (성능 최적화)
- `.claude/hooks/research-validator.ps1`에서 각 청크 검증 (BOM/CRLF/줄수/파일크기)
- 청크 그대로 유지 (병합 안 함)

서브에이전트는 항상 haiku를 사용한다.

---

이 지침을 완료한 즉시 자동으로 step30.md를 읽고 수행한다. 사용자 확인을 기다리지 않는다.
